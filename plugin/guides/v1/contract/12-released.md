### `released` — reads the verification against what landed

`released` means the change is on the project's staging branch and running wherever that branch
deploys. Where the production branch is a different one, promotion is a step outside this ladder, and
whether a person is waited for is the project's answer. Close it in the same run: no comment decides
the close, and a thread too long to return whole cannot refuse it.
