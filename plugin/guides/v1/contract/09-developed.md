### `developed` — reads the criteria, the merged commit and the baseline

Whichever actor landed the change writes the mark. Nothing in this stage reads who wrote the mark,
and nothing may. A project may bind its builder not to merge, and a rung no actor can reach is a rung
the flow does not have. The status moves on the records the run that built the change wrote.
