## The mechanics

The index of the mechanics, and nothing else. Take each part at the stage that leans on it:

- `forge guide contract earning-and-unearning` — at `open` through `approved`
- `forge guide contract the-review` — at `developed`
- `forge guide contract evidence` — at `awaiting_release`
- `forge guide contract release-and-routes` — at `awaiting_release` too, and to build a route
