### `clarified` — reads the decision record and the body

The next status promises *object to the plan now, not after*. Write the plan and the criteria before
the code, and have a second reader read them before the issue takes them. Never relax a criterion to
match what got built; correct it in the open instead. A correction that re-sizes the work climbs a
rung and never comes back down: the complexity is the reporter's claim, and what the work turned out
to be is the run's.
