### `open` — reads the title, body, comments, attachments and blocking relations

Nobody earns this one. Leave it on a confirmation whose finding holds, or on one carrying a
disposition: already fixed, duplicate, intended, obsolete, or a premise the repository disproves. A
disposition drops the issue with that finding as its reason; it does not jump past `confirmed`. An
issue that is several gets siblings filed `open` in one write, each naming the others, and is
confirmed as the first of them.
