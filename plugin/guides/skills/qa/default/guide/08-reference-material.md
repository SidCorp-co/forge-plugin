## Reference material

Read on arrival at the phase that cites it.

| Read | At |
|---|---|
| `forge guide qa judging` | Phase 3, and by every run dispatched from it |
| `forge next -h` | Phase 1, for what the queue carries and what bounds it |
| `forge doctor` | Phase 1, for the declaration and the number of runs |
| `forge advance <ref> --owed` | Phases 2 and 4, for what the next rung wants |
| `forge record -h`, `forge claim -h` | any write to the tracker |
