# the short lease — the write that no work follows

A lease is as long as the work under it, and some writes have no work under them at all. A reading
posted and the issue left — a triage pass's confirmation, or the complexity that same reading
judged — is the whole of what that run will ever do to the issue. A default-length lease over one of
those charges the run that comes next a reclaim for nothing, which is the cost a wave triages to
avoid, paid back with interest.

So the duration and the line saying nothing was worked are one sentence, and it is printed twice
from one place: by `forge claim -h`, and again by the refusal a payload write meets when it holds no
lease at all. The refusal is the surface that matters, because the run that needs this rule is the
run that has just been stopped by it. **Neither surface names a kind of write.** Nothing in the CLI
can tell a reading's output from a build's — the run is what knows whether work follows — so the
sentence describes the write's relation to the work and never the verdict behind it, and a route the
CLI grows that needs no lease at all wins over both.

**The measurement is the whole argument for stating it there.** The rule was decided and then
written down in one place only: a dispatch reference, under a heading about posting a disposition,
which is the one verdict a holding issue never reaches. On 2026-09-09 two triage runs given the same
instruction against the same head resolved it opposite ways. One claimed each issue for ten minutes,
wrote its confirmations and complexities, and recorded that nothing had been worked under the lease.
The other read the refusal as the boundary its role's no-reclaim sentence drew, declined to claim at
all, judged four issues and handed four complexities back to the dispatcher as recommendations,
which the dispatcher then wrote — four writes the reading had already earned, and a phase that could
not report itself complete. A third run got it right, and the only thing that separated it was a
brief carrying the rule in as many words (ISS-840).
